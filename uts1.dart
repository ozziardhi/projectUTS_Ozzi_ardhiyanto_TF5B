import 'dart:io';

main() {
  print("program input nilai");
  stdout.write("Nama:");
  stdin.readLineSync();
  stdout.write("Nilai UTS:");
  stdin.readLineSync();
  stdout.write("Kelas:");
  stdin.readLineSync();
  stdout.write("Mata Kuliah:");
  stdin.readLineSync();
}
